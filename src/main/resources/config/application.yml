server:
  port: ${PORT:5000}

spring:
  application:
    name: social_poll

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml

  mvc:
    hiddenmethod:
      filter:
        enabled: true